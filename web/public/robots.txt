# academic Ledger. Marketing / info pages are crawlable and indexable; the dynamic data pages are
# not — their QaL point estimates are illustrative pending calibration (shouldn't be indexed), and
# crawling them drives metered API work. NOTE: robots.txt governs only *compliant* crawlers; the
# budget protection against misbehaving bots is in code (out-of-index paper views cost zero API
# calls). Re-allow once calibration is broad and the corpus is served from our own store.
User-agent: *
Disallow: /paper/
Disallow: /author/
Disallow: /api/
