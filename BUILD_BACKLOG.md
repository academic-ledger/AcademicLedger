# Build backlog

Milestones map to QaL_spec.md §12 (POC / MVP / V1.0). Check items off as you go.

## POC — pages live, real observed percentiles, QaL illustrative  ✅ complete
- [x] **P1 Scaffold the web app.** Next.js (App Router, TypeScript) in `web/`; shared brand bar + nav; responsive to ~380px.
- [x] **P2 Port the mocks** into components/pages; one shared record-list component (`RecordTable`) across explore + author.
- [x] **P3 Read API** (`app/api/*`) per `API_CONTRACT.md`: `/api/qal/:oaid`, `/api/author/:oaid`, `/api/explore` — reads Neon.
- [x] **P4 Calibration-pending UX** — papers outside the calibrated communities show observed standing only, labeled honestly.
- [x] **P5 Deploy** — Vercel + custom domain (apex `academic-ledger.org` → `www`); renders on desktop and phone width.

## MVP — real headline metric for the seed, cached
- [x] **M1 Datastore.** Neon + `db/schema.sql` (`works`, `cohort_percentiles`, `calibration_models`, `qal_records`, `authors`, `author_works`, `synthetic_field`, `subfields`).
- [x] **M2 Ingest.** `pull_cohort.py`: **1800** full (~32k); **1803 & 1802** as 10k/cohort uniform random samples (full subfields are ~716k/~526k). `--sample` + batched upserts.
- [x] **M3 Percentile tables.** `build_percentiles.py` → `cohort_percentiles`, full-population **mid-rank** convention; min-cohort-size guard.
- [x] **M4 Calibrate Layer B.** `calibrate.py` pools vintages + per-age **split-conformal** widening (`calib_lib.py`). **Back-test passes** (`backtest.py`): ~0.90–0.91 leave-one-vintage-out coverage across 1803/1802/1800 (raw ~0.80 was overconfident; conformal fixed it).
- [x] **M5 Official reference class = the synthetic field.** `synthetic_field.py` (QaL_spec §3/§5): per paper, staged topic-mixture weights — reference-based recency stage (research front) migrating to the co-citation community as citations accrue (λ = c/(c+k)) — then rank the paper against the **full-population blend** of those (subfield, vintage) cohorts via **exact count queries** (uncited atom at mid-rank 100·p₀/2). The earlier cited-only co-citation-*neighborhood* ranking was a flaw (it put a top paper at the ~22nd percentile) and has been **deleted** — nothing ranks against a cited-only/popularity-biased set. Regression test `pipeline/test_percentile.py` pins the rule. Prefilled the top ~480 seed/leaderboard papers (cached; serving reads it). **Update (2026-06-11):** the synthetic field is now computed **on the fly for any paper** via the full fallback chain **references → co-citation → content-and-authorship prior** (`web/lib/synthetic.ts` + `pipeline/synthetic_field.py`), correcting OpenAlex's single label even for brand-new SSRN papers with no refs and no citers; a pre-synthetic cached row is upgraded to the live synthetic field on read. Whitepaper + spec updated (`docs/QaL_spec.md` §5, `docs/QaL_whitepaper.md`). **Prefill coverage (2026-06-15):** Explore's "Fields" column reads the **cached** `synthetic_field` blend only (one query per page; no live OpenAlex per row), whereas the paper page computes the blend **live** (`syntheticQal`) — so an uncached paper showed "blend pending" on Explore even though its paper page rendered the full blend. Expanded the prefill from the ~527 seed/leaderboard set to the **top ~2,000 papers by citations** (the browsed set) via `synthetic_field.py --oaids-file` (new flag; the script now loads the repo-root `.env`). Backfill ran on the **free polite pool** while the premium budget was drained, then switched to premium once refunded.
- [~] **M6 Compose + serve.** `compute_qal.py` joins works + `cohort_percentiles` + `calibration_models` + `synthetic_field` → `qal_records` (carries **both** field and synthetic metrics); API reads through from Neon. **Still to do: wire the monthly cron** (`.github/workflows/refresh.yml`) to run pull → percentile → calibrate → compute (+ synthetic). *(A launchd one-shot currently resumes the synthetic prefill after the daily OpenAlex quota resets — a stopgap, not the cron.)*
- [ ] **M7 Retraction overlay** (Retraction Watch via Crossref) + the Zenodo "deposit a record" affordance.
- [x] **M8 Paper page parameter-aware** — `paper/[oaid]` loads the clicked work.
- [x] **M9 On-the-fly author pages + clickable bylines — make the author page work for any author** (QaL_spec §9/§11). **Done:** bylines carry per-author `{name, oaid, orcid}` (live `mapAuthorships` in `openalex.ts`; `pull_cohort._row` + `compute_qal` fold them into `display.authorships`); a shared `Byline` component links each identified author on the paper page and in `RecordTable` (plain-text fallback when no id, or for cached records not yet re-pulled). `getAuthorRecord` builds the author page **on-the-fly for any author** (live `fetchAuthor` + `fetchAuthorWorks`, joined to cached QaL, universal-layer obs computed in one batched CDF query), with the seeded-author DB fallback — verified live on Cachon/Duckworth. Explore-table links populate as the cache refreshes; the paper + author pages link immediately. Original spec text below, for reference:
  1. **Identity-carrying bylines.** Capture per-author `{name, oaid, orcid}` at fetch/ingest time instead of discarding the IDs into a flat string — change `byline()` in `web/lib/openalex.ts` and `_row` in `pipeline/pull_cohort.py` to keep an `authorships` array (store in `display.authorships`; keep the abbreviated string for compact display). Then render each *displayed* author name as a `<Link href="/author/{oaid}">` on the paper page (`paper/[oaid]/page.tsx`) and in `RecordTable`.
  2. **On-the-fly author page.** Construct `/author/[oaid]` live for ANY author (mirroring `getPaperRecord`): `fetchAuthor(oaid)` (entity) + their works via OpenAlex, each work shown with cached QaL where computed (`qal_records`) and universal/pending otherwise — no pre-seeding. Replace the DB-only `getAuthor` (which needs `authors`/`author_works`) with a live `getAuthorRecord` that falls back to the stored author when the live fetch fails, exactly like the paper page. Retires the old `author_ingest.py` DB-seeding plan. Keep the **no single rolled-up author score** rule (per-paper distribution only). Caveat: arbitrary authors span subfields we haven't calibrated, so many works show universal/pending until coverage broadens.

- [~] **M10 Caching architecture — cache computed records, don't persist the raw corpus** (`docs/caching_architecture.md`, Option A). **Done:** the paper page constructs live (single-entity OpenAlex + cached QaL); `qal_records` gained a `display` JSONB so serving (explore, author, paper-fallback) no longer reads the raw `works` snapshot — `works` is now pure compute-staging. **Remaining (needs the OpenAlex list-quota back + a streaming refactor):** rebuild `cohort_percentiles` by streaming cohorts at refresh, pull calibration cohorts transiently, then **drop the `works` table**; fold into the M6 monthly cron. Until then `works` stays as harmless staging and display self-heals on the next pull + `compute_qal`.

- [~] **M11 Stage-one coverage rollout (OID department)** — QaL_spec §11 staged sampling (`docs/coverage_rollout.md`). **Built & scheduled:** `pipeline/coverage_rollout.py` (resumable, budget-aware) runs footprint → skeleton percentile tables → uniform-sample interiors → **hybrid** calibration (proven nonparametric `calib_lib` where matured data allows; `calib_parametric` half-life+tail fallback, §7 shrinkage, otherwise); confidence tiers (`parametric|fitted|gate-passed`) carried to every served record via `compute_qal` (forecast gated to `gate-passed`; UI names the tier). Migration `002` adds `cohort_percentiles.source`, `calibration_models.confidence` (seed → `gate-passed`), and the `coverage_progress` checkpoint. Daily launchd job `com.academicledger.coverage` (`run_coverage.sh`, 21:30 local) fires after the synthetic backfill and resumes nightly until coverage is satisfactory. Deferred refinements (works-level recency-weighted footprint, research-grade parametric model, group_by tail completeness) noted in the doc. **Coverage update (2026-06-11):** gate-passed now covers **71%** of department reference-class weight (communities 12 → 20). The conformal radius was reworked to a **cross-vintage (leave-one-vintage-out) estimate** (`calib_lib.conformal_q_cv`) — fixing high-impact subfields that were *under-covered* (0.44–0.86 LOVO coverage), not under-sampled — and the conformal target tightened to **α=0.08**; 8 subfields flipped to gate-passed this session. A deterministic **residual cache** (`data/calib_cache/`, `--from-cache`) re-tunes the widening offline with no OpenAlex re-pull. Economics stays honestly pending (two structurally anomalous vintages). Full method decision log in `docs/calibration_decisions.md`; the nightly job re-runs the whole set with the new code/α.

## Display & UX
- [x] **U1 "Working" indicator.** Route `loading.tsx` skeletons + top route-transition bar; explore fetch shows a skeleton table + spinner; shimmer honors `prefers-reduced-motion`.
- [x] **U2 Dual-metric, sortable columns.** `qal_records.metrics` carries both; the shared table shows **`QaL · field`** and **`QaL · synthetic ★`** as sortable columns (official highlighted, "—" where not yet computed). No headline toggle — **column sorting** is the exploration affordance; the official number stays the synthetic field (§3, anti-shopping). *(Explore search/filter now run server-side over the full DB, not a pre-fetched slice.)*
- [x] **U3 Synthetic-field composition on the paper record.** Ranked subfield-weight bars from `synthetic_field.weights`, showing the paper's true intellectual blend vs OpenAlex's single label ("divergence is the signal"). Names come from the dynamic `subfields` table; `pipeline/fetch_subfields.py` completes the ~250-name taxonomy (wired into the scheduled backfill, so remaining names auto-fill — no redeploy).
- [x] **U4 Full citation on the paper page.** Formatted reference (authors · year · title · venue · DOI) + copy button. **Enrichment TODO:** volume/issue/pages from OpenAlex `biblio` (capture in `pull_cohort._row` + backfill); optional BibTeX/RIS export.
- [x] **U5 Drop the Access cell.** Done — the OA-status (Open/Closed) cell is removed from the paper-page evidence grid; the remaining 5 cells flow to a clean 3×2 with the freed slot empty. `is_oa`/`oa_url` kept in the data layer.
- [x] **U6 Composition as a flat strip (no graphic).** Done — the ranked-bar card is replaced by a compact full-width strip under the QaL hero: label ("Synthetic field · weighted by recent references") + flat "Subfield name (NN%)" list (descending, middot-separated), long tail collapsed into "+N more (X%)", divergence note kept. The `.comp*` bar CSS was removed.
- [x] **U9 Vintage dropdown has gaps (2024, 2025, … missing).** Done — the "Vintage (since)" `<select>` now renders a fixed continuous range (`VINTAGES` = `AS_OF` 2026 down to `MIN_VINTAGE` 2000) instead of the union of years present in the loaded rows, so every vintage is selectable regardless of which records loaded. Dropped the result-driven `years` state/accumulation.
- [x] **U8 Live search fallback for explore.** Done — when a text search returns nothing from the index, explore falls back to a **live OpenAlex search** (`searchWorks` in `web/lib/openalex.ts`: a title search AND a `raw_author_name.search`, merged, sorted by cites; respects the field + vintage filters). Hits render with the universal layer (observed field percentile when in a built cohort, else calibration-pending — QaL columns read "pending"); a yellow banner in `ExploreClient` explains they're live, not indexed. Fixes the "famous author outside the 10k/cohort sample returns zero" gap (e.g. Cachon). Quota note: the /works **list** endpoint shares OpenAlex's daily budget (unlike single-entity fetch), so results are cached via Next fetch revalidation (1h) and any non-OK response degrades to the empty state. Shares the live-fetch mapper with the paper page; distinct from M9 (author-page construction).

- [x] **U7 About: transparency note + links (repo + white paper).** Done — About page carries the transparency note ("academic Ledger is an independent, non-commercial initiative…") linking the GitHub repo and the white paper; the paper-page footer links the white paper too ("How QaL works"). The white paper is rendered from `docs/QaL_whitepaper.md` to `web/public/whitepaper.html` (served on-site, like talk.html) via `pipeline/render_whitepaper.py`.


- [x] **U10 Explore "Fields" column shows the synthetic blend + paper-page field/venue cleanup.** Done — the Explore/author "Fields" column renders the synthetic-field blend (dominant subfield short-label + weight + "+N more", full blend on hover) instead of OpenAlex's single subfield, with an italic "· blend pending" fallback when the cache has no blend yet and a plain single-label fallback when the composition wasn't fetched for that view; header "Field"→"Fields"; the redundant "calibrated" pill was dropped. Short labels come from a hand-edited 252-subfield table (`docs/subfield_abbreviations.md` → `web/lib/subfieldShort.ts` via `pipeline/build_subfield_short.py`). On the paper page the bare "synthetic field" label pill became the same Top-1+N field pill, and the venue is now **italic** (no pill) to disambiguate it from a subfield name (e.g. the journal "Management Science").

## V1.0 — the Lens at scale
- [ ] **Author-curated overrides (verified overlay).** OpenAlex author metadata is often wrong (e.g. Joseph Simmons shown at "California University of Pennsylvania"; affiliations/clusters glitch). Let a subscriber **claim and edit their own author record** — a *check/override layer on our side* that supersedes selected OpenAlex fields (affiliation, display name, maybe work attribution add/remove) with author-verified values at serve time. **Does not write back to OpenAlex.** Mechanics: gate the claim via **ORCID auth** (the verified-identity anchor already noted in the author-page footer); store overrides in an `author_overrides` table keyed by OpenAlex Author ID; `getAuthorRecord` merges overrides over the live OpenAlex entity (override wins, with a small "author-verified" marker for transparency). Keep it auditable (who edited what, when) and reversible. Distant-future; pairs naturally with accounts/subscribers and the eventual community layer. **Partially built:** the **work-disavowal ("NOT ME")** half of this is live server-side — `author_excluded_works` (migration 003) lists works wrongly clustered onto an author, and `getAuthorRecord` drops them (seeded manually for now, e.g. the German-language Karl T. Ulrich book). Remaining: the member-facing **"NOT ME" button** on each row + the ORCID-gated claim flow, and the field-override half (affiliation/name).
- [ ] **Author notes on paper records (verified annotations).** Let a subscribing, ORCID-verified author of a paper attach a short **note** to its record — e.g. "Please see the more recent *Management Science* article, which supersedes this working paper," a correction, or added context. Shown on the paper page as an attributed, **"author-verified"** annotation (distinct from the computed QaL, which it never alters), with the noting author's name linking to their author page. Mechanics: `paper_notes` table keyed by Work ID + author OpenAlex ID; the claim is gated by the same ORCID auth as author overrides, and limited to verified authors of that work; auditable (who/when) and editable/removable by the author. A natural superseding-version link could later auto-surface the newer DOI. Sibling to the author-curated overrides item; both are the author-side of the eventual community layer.
- [ ] **Membership + badge ("aL Guild" / "aL Member"), dues-funded.** A membership tier shown as a small badge next to an author's name (author page, and optionally bylines), signalling support and verified participation. Annual dues (~$XX/yr) as the **sustainability model** — enough to float the project (OpenAlex premium, hosting, Neon) and **keep it independent and non-commercial** (a member-supported guild, not ads or data-selling; aligns with the About page framing). Membership is the gate for the author-side features above — claim/curate your record (overrides) and add paper notes — and the ORCID-verified identity anchor. Open question: naming ("Guild" vs "Member" vs "Subscriber"), free vs paid tiers (reading always free; curation/badge for members), and governance. Payment via a standard processor (Stripe); no financial data touches our store.
- [ ] Ingest the OpenAlex bulk snapshot into the owned store — drops the live-API dependency and its **daily quota**, which currently bounds the per-paper synthetic-field prefill.
- [ ] Full-corpus cohort tables, synthetic fields, and authority-weighting (PageRank).
- [ ] Public read API; QaL-ranked leaderboards as a public good; calibrate more communities (e.g. Applied/Social Psychology, a computational-social-science bundle).
- [ ] Coverage limits (law, humanities) reported honestly; manipulation-robustness audited.

## Decisions log (so they are not relitigated)
- **Official reference class = the synthetic field** — a recency-weighted topic-mixture blend of full (subfield, vintage) cohorts (reference-based → community migration). The cited-only co-citation *neighborhood* is **not** a ranking cohort (it's popularity-biased and has no uncited papers); it only informs the topic mixture. (QaL_spec §3/§5.)
- **Denominator rule:** every percentile is over the **full population** (cited + uncited), never cited-only and never a top-N/sorted sample; the uncited atom takes the **mid-rank 100·p₀/2**; the same convention is used for r_obs and r∞.
- Calibration seed = Decision Sciences subfields **1803, 1802, 1800** (extensions 1405, 1804 available).
- QaL = calibrated posterior over eventual percentile (point + 90% interval + bucket probs); back-tested to ~90% coverage via per-age split-conformal.
- **No single author-level score** (author page shows a per-paper distribution).
- Stack = GitHub (batch) + Vercel/Next.js (web + read API) + Neon Postgres; **Vercel does no heavy compute**.
- **Synthetic-field construction is a fallback chain** (references → co-citation → content-and-authorship prior → single label), computed on the fly for any paper, so OpenAlex's single label is never the official reference class. (`docs/QaL_spec.md` §5.)
- **Layer-B conformal radius = cross-vintage (LOVO) at α=0.08.** A single fit/cal split under-covered held-out vintages; the cross-vintage radius fixes it and α=0.08 targets the honest ~90% actual coverage. Gate unchanged (0.88 ≤ LOVO coverage ≤ 0.97). A subfield that genuinely under-covers (e.g. Economics: two anomalous vintages) stays `fitted`/pending rather than being forced over — honesty over coverage. (`docs/calibration_decisions.md`.)
- **Explore is cache-only for the synthetic blend; the paper page computes it live.** Explore reads the cached `synthetic_field` table (one batched query, no per-row OpenAlex call) to keep page loads fast and off the API budget; coverage of the blend on Explore is therefore a *prefill* question (currently the top ~2,000 by citations), not a live computation. The paper page computes the blend on the fly for any paper, so the two can diverge until the prefill catches up.
